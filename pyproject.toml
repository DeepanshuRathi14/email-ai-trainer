[project]
name = "email-ai-trainer"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "python-multipart",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"